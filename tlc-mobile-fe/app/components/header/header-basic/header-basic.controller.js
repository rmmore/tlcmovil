/**
 * Esta clase controladora manejará todos los procesos 
 * que estan conectados hacia la directiva 'header-basic' y 
 * las vistas html que se dispone.
 *
 * Capa de Componentes
 *
 * @author Rolando Paredes Alzamora (rparedea@everis.com)
 * 
 */
 
(function(angular) {
	'use strict';

	angular
		.module('header-basic.controller', [])
		.controller('HeaderBasicController', HeaderBasicController);
		
		HeaderBasicController.$inject = [
		
		];
		
		function HeaderBasicController (
		
		) {
			// TODO: Implementar controlador
		}

}(angular));